[tool.poetry]
name = "common-benefits-sdk"
version = "0.0.0"
description = "Python SDK for the CommonBenefits protocol: codegen-free extensions, a typed client, and custom filters."
authors = ["Agile Six"]
readme = "README.md"
packages = [{ include = "common_benefits_sdk" }]

[tool.poetry.dependencies]
python = "^3.11"
pydantic = "^2.11.0"
typing-extensions = "^4.12.0"
httpx = ">=0.27,<0.29"

[tool.poetry.group.dev.dependencies]
black = "^24.0.0"
pyright = "^1.1.380"
pytest = "^8.0.0"
ruff = ">=0.6,<0.16"

[tool.pyright]
pythonVersion = "3.11"
typeCheckingMode = "basic"
include = ["common_benefits_sdk", "examples", "tests"]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

/**
 * CommonBenefits TypeScript SDK
 *
 * Provides TypeScript types and Zod schemas for the CommonBenefits protocol.
 * Import the subpaths you need:
 *
 * - `@common-benefits/sdk/schemas` for Zod validators.
 * - `@common-benefits/sdk/types` for the inferred TypeScript types.
 * - `@common-benefits/sdk/constants` for enum runtime values.
 *
 * @packageDocumentation
 */
